<!-- <h1>Products by Category</h1>

<% @categories.each do |category| %>
  <%= category.name %>
  <% category.products.each do |product| %>
    <%=image_tag(product.picture, :alt => "product photo", :title => "Product")%>
  <% end %>
<% end %> -->
