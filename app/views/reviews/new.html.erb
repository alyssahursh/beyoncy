<div class="format-forms">
  <h1>Add a Review</h1>
  <p class="flash-notice"><%= render partial: 'shared/flash_notice' %></p>
  <%= render partial: "reviewform", method: :post, locals: { action_name: product_reviews_path, params: {product_id: params[:product_id] } } %>
</div>
